{
  "name": "cyberstore",
  "version": "1.0.0",
  "description": "Full-stack E-Commerce Application",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install",
    "start": "cd backend && node seedProducts.js && node server.js",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "install-deps": "npm install --prefix frontend && npm install --prefix backend"
  },
  "keywords": ["ecommerce", "mern", "react", "express", "mongodb"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
